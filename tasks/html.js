// ============================================================
// HTML TASKS
// ============================================================

export const HTML_TASKS = [
  // ── HTML ЗАДАНИЕ 1: Теги и структура ──────────────────────
  {
    id: 'html-1-tags',
    module: 'html',
    moduleIndex: 1,
    title: 'HTML: Первый день — сломанная страница',
    npc: 'boss',
    context: `
      🗂️ <b>Первый рабочий день в «Кодликсе».</b><br><br>
      Тимлид Артём встречает тебя у входа с кислой миной:<br>
      <i>«Дима, хорошо что пришёл. Предыдущий стажёр сломал страницу приветствия для новых сотрудников.
      Теги везде перепутаны — браузер рендерит какую-то кашу. Почини до обеда, пожалуйста.»</i><br><br>
      Ты садишься за компьютер. Пора разобраться с HTML.
    `,
    introDialog: [
      'Дима, добро пожаловать в первый рабочий день. Начнём с небольшой, но очень настоящей аварии на странице приветствия.',
      'Предыдущий стажёр писал HTML быстро и перепутал закрывающие теги. Браузер пытается догадаться, что он имел в виду, поэтому страница выглядит странно.',
      'Твоя задача — найти три места, где открывающий тег по ошибке повторили вместо закрывающего. Особенно внимательно проверь h1, p и b.',
      'Сначала разберись с теорией про теги и вложенность, затем открой код и исправь разметку так, чтобы браузер понял структуру без догадок.',
    ],
    brief: 'Исправь три закрывающих тега: у <code>&lt;h1&gt;</code>, одного <code>&lt;p&gt;</code> и <code>&lt;b&gt;</code> должен появиться слэш <code>/</code>.',
    theory: `
      <b>HTML — язык смысла и структуры</b><br><br>
      HTML нужен не для красоты, а для разметки: он объясняет браузеру, какие части страницы являются
      заголовками, абзацами, ссылками, списками и другими элементами. Если сравнить страницу с газетой,
      то HTML отвечает за рубрики, заголовки, текстовые блоки и порядок материалов.<br><br>

      <b>Тег</b> — это команда разметки в угловых скобках. У большинства тегов есть открывающая и
      закрывающая часть:<br>
      <pre><code>&lt;h1&gt;Заголовок страницы&lt;/h1&gt;
&lt;p&gt;Обычный абзац текста&lt;/p&gt;</code></pre>
      В закрывающем теге обязательно есть слэш: <code>&lt;/p&gt;</code>. Без него браузер может решить,
      что следующий текст всё ещё относится к старому элементу.<br><br>

      <b>Частые теги текста:</b><br>
      <code>&lt;h1&gt;</code> — главный заголовок страницы, обычно один самый важный;<br>
      <code>&lt;h2&gt;</code> — заголовок раздела;<br>
      <code>&lt;p&gt;</code> — абзац;<br>
      <code>&lt;b&gt;</code> — визуально жирное выделение;<br>
      <code>&lt;i&gt;</code> — визуальный курсив.<br><br>

      <b>Вложенность</b> означает, что элементы могут находиться внутри других элементов. Главное правило:
      тег, который открыли последним, закрывается первым. Это похоже на папки на компьютере: нельзя закрыть
      внешнюю папку, пока внутри неё всё ещё открыта внутренняя.<br>
      <pre><code>&lt;p&gt;Здесь &lt;b&gt;важный текст&lt;/b&gt; внутри абзаца.&lt;/p&gt;</code></pre>

      <b>Типичная ошибка новичка:</b><br>
      <pre><code>&lt;h1&gt;Привет&lt;h1&gt;   &lt;!-- второй тег ошибочно открыт заново --&gt;
&lt;p&gt;Текст&lt;p&gt;      &lt;!-- нужен закрывающий тег &lt;/p&gt; --&gt;</code></pre>
      Правильно: <code>&lt;h1&gt;Привет&lt;/h1&gt;</code> и <code>&lt;p&gt;Текст&lt;/p&gt;</code>.
    `,
    description: `
      ✏️ <b>Задание:</b> Исправь страницу приветствия.<br><br>
      В коде три ошибки: у некоторых тегов пропущен закрывающий слэш <code>/</code>.
      Найди и исправь все три тега — <code>&lt;h1&gt;</code>, <code>&lt;p&gt;</code> и <code>&lt;b&gt;</code>.
    `,
    starterCode: `<h1>Добро пожаловать в Кодликс!<h1>
<p>Мы рады видеть тебя в нашей команде.<p>
<p>Здесь ты будешь <b>создавать крутые сайты<b> каждый день.</p>`,
    check(code) {
      const d = document.createElement('div');
      d.innerHTML = code;
      const h1 = d.querySelector('h1');
      const ps = d.querySelectorAll('p');
      const b = d.querySelector('b');
      if (!h1 || !h1.textContent.trim())
        return { ok: false, hint: 'Тег &lt;h1&gt; не закрыт. Добавь &lt;/h1&gt; в конце заголовка.' };
      if (ps.length < 2)
        return { ok: false, hint: 'Один из тегов &lt;p&gt; не закрыт. Добавь &lt;/p&gt; в конце абзаца.' };
      if (!b || !b.textContent.trim())
        return { ok: false, hint: 'Тег &lt;b&gt; не закрыт. Добавь &lt;/b&gt; после выделенного текста.' };
      return { ok: true };
    },
    reward: '🏅 Первый баг исправлен! Артём кивает с одобрением.',
    colleagueTips: [
      { npc: 'colleague1', lines: [
        'Привет, я Маша! Добро пожаловать в команду.',
        'Смотри, закрывающий тег — это как крышка: &lt;h1&gt; открыл, &lt;/h1&gt; закрыл. Слэш — главное отличие.',
        'Пройдись по каждому тегу и проверь: есть ли у него пара с &lt;/...&gt;?',
      ]},
      { npc: 'colleague2', lines: [
        'Саша. Коротко: ищи теги без слэша в конце — это и есть баги.',
        'Например &lt;h1&gt;Текст&lt;h1&gt; — второй тег должен быть &lt;/h1&gt;.',
        'Три тега сломаны: h1, один из p, и b. Найди их и добавь слэш.',
      ]},
    ],
  },

  // ── HTML ЗАДАНИЕ 2: Атрибуты ──────────────────────────────
  {
    id: 'html-2-attributes',
    module: 'html',
    moduleIndex: 2,
    title: 'HTML: Ссылки и атрибуты',
    npc: 'boss',
    context: `
      🔗 <b>Артём снова у твоего стола:</b><br><br>
      <i>«Дима, на странице "О компании" есть ссылки на соцсети, но они никуда не ведут.
      Кто-то забыл прописать адреса. И ещё — картинка не отображается, у неё нет пути к файлу.
      Разберись, пожалуйста.»</i><br><br>
      Ты открываешь файл и видишь пустые атрибуты. Время разобраться, что такое атрибуты в HTML.
    `,
    introDialog: [
      'Дима, у нас страница “О компании”, но ссылка на сайт сейчас просто текст, а логотип вообще не загружается.',
      'Проблема не в самих тегах, а в их настройках. Ссылке нужен адрес, картинке нужен путь к файлу и нормальное текстовое описание.',
      'В HTML такие настройки называются атрибутами. Они пишутся внутри открывающего тега и объясняют браузеру, как элемент должен работать.',
      'После теории добавь href для ссылки, а для картинки пропиши src и alt. Это уже похоже на реальную правку в проекте.',
    ],
    brief: 'Добавь ссылке <code>href="https://kodlix.ru"</code>, а картинке атрибуты <code>src="logo.png"</code> и <code>alt="Логотип Кодликс"</code>.',
    theory: `
      <b>Атрибуты — настройки HTML-элемента</b><br><br>
      Тег говорит браузеру, <i>что</i> это за элемент, а атрибуты уточняют, <i>как именно</i> он должен
      работать. Атрибут пишется в открывающем теге по схеме <code>имя="значение"</code>:<br>
      <pre><code>&lt;тег атрибут="значение"&gt;текст&lt;/тег&gt;</code></pre>
      Значение лучше писать в кавычках. Так код легче читать, и браузер не ошибётся, если внутри значения
      появятся специальные символы или пробелы.<br><br>

      <b>Ссылка: тег &lt;a&gt;</b><br>
      Ссылка без адреса выглядит как текст и никуда не ведёт. За адрес отвечает атрибут
      <code>href</code>:<br>
      <pre><code>&lt;a href="https://kodlix.ru"&gt;Открыть сайт&lt;/a&gt;</code></pre>
      Текст между <code>&lt;a&gt;</code> и <code>&lt;/a&gt;</code> — это то, по чему пользователь нажимает.
      В реальных проектах текст ссылки должен быть понятным: лучше «Открыть расписание», чем просто
      «тут».<br><br>

      <b>Картинка: тег &lt;img&gt;</b><br>
      <code>&lt;img&gt;</code> — одиночный тег: у него обычно нет закрывающей пары. Ему нужны два важных
      атрибута:<br>
      <code>src</code> — путь к файлу картинки;<br>
      <code>alt</code> — текстовое описание картинки.<br>
      <pre><code>&lt;img src="logo.png" alt="Логотип Кодликс"&gt;</code></pre>

      <b>Зачем нужен alt?</b><br>
      Если картинка не загрузится, браузер покажет текст из <code>alt</code>. Ещё этот текст читают
      программы экранного доступа для людей с нарушением зрения. Поэтому <code>alt</code> — не украшение,
      а часть качества сайта.<br><br>

      <b>Запомни:</b> атрибуты всегда находятся внутри открывающего тега:
      <code>&lt;a href="..."&gt;</code>, <code>&lt;img src="..." alt="..."&gt;</code>.
    `,
    description: `
      ✏️ <b>Задание:</b> Исправь страницу «О компании».<br><br>
      1. Добавь атрибут <code>href="https://kodlix.ru"</code> в тег <code>&lt;a&gt;</code>.<br>
      2. Добавь атрибут <code>src="logo.png"</code> и <code>alt="Логотип Кодликс"</code> в тег <code>&lt;img&gt;</code>.
    `,
    starterCode: `<h2>О компании Кодликс</h2>
<p>Мы делаем интернет лучше с 2010 года.</p>
<a>Наш сайт</a>
<img>`,
    check(code) {
      const d = document.createElement('div');
      d.innerHTML = code;
      const a = d.querySelector('a');
      const img = d.querySelector('img');
      if (!a) return { ok: false, hint: 'Тег &lt;a&gt; не найден.' };
      if (!a.getAttribute('href') || !a.getAttribute('href').startsWith('http'))
        return { ok: false, hint: 'Добавь атрибут href со ссылкой, например: href="https://kodlix.ru"' };
      if (!img) return { ok: false, hint: 'Тег &lt;img&gt; не найден.' };
      if (!img.getAttribute('src'))
        return { ok: false, hint: 'Добавь атрибут src к тегу &lt;img&gt;, например: src="logo.png"' };
      if (!img.getAttribute('alt'))
        return { ok: false, hint: 'Добавь атрибут alt к тегу &lt;img&gt; — это важно для доступности!' };
      return { ok: true };
    },
    reward: '🏅 Атрибуты освоены! Ссылки заработали.',
    colleagueTips: [
      { npc: 'colleague1', lines: [
        'Маша здесь! Атрибуты — это как настройки тега.',
        'Для ссылки: &lt;a href="адрес"&gt;текст&lt;/a&gt;. Без href ссылка никуда не ведёт.',
        'Для картинки: &lt;img src="файл.jpg" alt="описание"&gt;. Оба атрибута обязательны!',
      ]},
      { npc: 'colleague2', lines: [
        'Саша. Атрибуты пишутся внутри открывающего тега через пробел.',
        'href для ссылок, src для картинок — запомни эти два, они нужны постоянно.',
        'alt у картинки — не просто формальность, это важно для людей с нарушением зрения.',
      ]},
    ],
  },

  // ── HTML ЗАДАНИЕ 3: Списки ────────────────────────────────
  {
    id: 'html-3-lists',
    module: 'html',
    moduleIndex: 3,
    title: 'HTML: Список задач команды',
    npc: 'colleague1',
    context: `
      📋 <b>Маша подходит к тебе с ноутбуком:</b><br><br>
      <i>«Дима, я делаю страницу с задачами нашей команды на спринт.
      Написала текст, но он выглядит как сплошная каша — всё в одну строку.
      Помоги оформить это нормальными списками? Артём хочет видеть маркированный список задач
      и нумерованный список приоритетов.»</i><br><br>
      Ты берёшься за дело.
    `,
    introDialog: [
      'Дима, Маша принесла страницу со списком задач на спринт. Сейчас это просто текст, который трудно быстро просмотреть.',
      'Команде нужны два разных списка: обычный список задач и список приоритетов, где порядок уже важен.',
      'В HTML для этого есть ul, ol и li. Если использовать их правильно, страница станет понятнее и для человека, и для браузера.',
      'Изучи короткую теорию, затем собери маркированный список из трёх задач и нумерованный список из двух приоритетов.',
    ],
    brief: 'Создай <code>&lt;ul&gt;</code> с 3 пунктами задач и <code>&lt;ol&gt;</code> с 2 пунктами приоритетов. Каждый пункт — через <code>&lt;li&gt;</code>.',
    theory: `
      <b>Списки помогают не превращать текст в кашу</b><br><br>
      Когда на странице несколько однородных пунктов, их лучше оформлять списком. Так пользователю проще
      быстро просмотреть информацию, а браузер и поисковые системы понимают, что это именно набор пунктов,
      а не случайные строки текста.<br><br>

      <b>Маркированный список: &lt;ul&gt;</b><br>
      Используется, когда порядок пунктов не важен: список задач, функций, покупок, участников команды.
      Браузер обычно показывает такие пункты с маркерами.<br>
      <pre><code>&lt;ul&gt;
  &lt;li&gt;Починить баги&lt;/li&gt;
  &lt;li&gt;Обновить кнопки&lt;/li&gt;
  &lt;li&gt;Написать документацию&lt;/li&gt;
&lt;/ul&gt;</code></pre>

      <b>Нумерованный список: &lt;ol&gt;</b><br>
      Используется, когда порядок важен: инструкция, рейтинг, план действий, этапы решения задачи.
      Браузер автоматически поставит номера.<br>
      <pre><code>&lt;ol&gt;
  &lt;li&gt;Открыть файл&lt;/li&gt;
  &lt;li&gt;Найти ошибку&lt;/li&gt;
  &lt;li&gt;Проверить результат&lt;/li&gt;
&lt;/ol&gt;</code></pre>

      <b>Пункт списка: &lt;li&gt;</b><br>
      Каждый пункт внутри <code>&lt;ul&gt;</code> или <code>&lt;ol&gt;</code> оформляется тегом
      <code>&lt;li&gt;</code>. Правильная структура такая: список снаружи, пункты внутри.<br>
      <pre><code>&lt;ul&gt;
  &lt;li&gt;Пункт внутри списка&lt;/li&gt;
&lt;/ul&gt;</code></pre>

      <b>Чего не стоит делать:</b> не размещай <code>&lt;li&gt;</code> отдельно от списка. Пункт списка
      должен жить внутри <code>&lt;ul&gt;</code> или <code>&lt;ol&gt;</code>.
    `,
    description: `
      ✏️ <b>Задание:</b> Оформи страницу задач команды.<br><br>
      1. Создай <b>маркированный список</b> <code>&lt;ul&gt;</code> с тремя задачами:<br>
      &nbsp;&nbsp;— «Починить баги на главной»<br>
      &nbsp;&nbsp;— «Обновить дизайн кнопок»<br>
      &nbsp;&nbsp;— «Написать документацию»<br><br>
      2. Создай <b>нумерованный список</b> <code>&lt;ol&gt;</code> с двумя приоритетами:<br>
      &nbsp;&nbsp;— «Критические баги»<br>
      &nbsp;&nbsp;— «Новые фичи»
    `,
    starterCode: `<h2>Задачи на спринт</h2>

<!-- Сделай маркированный список задач -->

<h2>Приоритеты</h2>

<!-- Сделай нумерованный список приоритетов -->
`,
    check(code) {
      const d = document.createElement('div');
      d.innerHTML = code;
      const ul = d.querySelector('ul');
      const ol = d.querySelector('ol');
      const liInUl = ul ? ul.querySelectorAll('li') : [];
      const liInOl = ol ? ol.querySelectorAll('li') : [];
      if (!ul) return { ok: false, hint: 'Создай маркированный список: &lt;ul&gt;...&lt;/ul&gt;' };
      if (liInUl.length < 3) return { ok: false, hint: `В маркированном списке должно быть 3 пункта &lt;li&gt;. Сейчас: ${liInUl.length}.` };
      if (!ol) return { ok: false, hint: 'Создай нумерованный список: &lt;ol&gt;...&lt;/ol&gt;' };
      if (liInOl.length < 2) return { ok: false, hint: `В нумерованном списке должно быть 2 пункта &lt;li&gt;. Сейчас: ${liInOl.length}.` };
      return { ok: true };
    },
    reward: '🏅 Списки освоены! Маша довольна — страница выглядит аккуратно.',
    colleagueTips: [
      { npc: 'colleague1', lines: [
        'Маша здесь! Это моё задание, так что объясню.',
        'ul — это неупорядоченный список (unordered), ol — упорядоченный (ordered). Внутри обоих — теги li.',
        'Структура: &lt;ul&gt;&lt;li&gt;пункт&lt;/li&gt;&lt;li&gt;пункт&lt;/li&gt;&lt;/ul&gt;',
      ]},
      { npc: 'colleague2', lines: [
        'Саша. Запомни: ul = маркеры, ol = цифры, li = один пункт.',
        'Не забудь закрыть каждый &lt;li&gt; и сам список &lt;/ul&gt; или &lt;/ol&gt;.',
        'Пункты должны быть ВНУТРИ тега ul или ol, не снаружи.',
      ]},
    ],
  },

  // ── HTML ЗАДАНИЕ 4: Таблицы ───────────────────────────────
  {
    id: 'html-4-tables',
    module: 'html',
    moduleIndex: 4,
    title: 'HTML: Таблица сотрудников',
    npc: 'colleague2',
    context: `
      📊 <b>Саша перехватывает тебя у кофемашины:</b><br><br>
      <i>«О, Дима! Слушай, мне нужна помощь. Артём попросил сделать таблицу сотрудников
      для внутреннего портала — имя, отдел, email. Я начал, но запутался в тегах таблицы.
      Можешь доделать? Там осталось добавить строки с данными.»</i><br><br>
      Саша показывает незаконченный код. Ты берёшь задачу.
    `,
    introDialog: [
      'Дима, Саша начал таблицу сотрудников для внутреннего портала, но остановился на заголовках.',
      'Нужно не просто написать текст в несколько строк, а оформить данные как таблицу: строки, ячейки, заголовки столбцов.',
      'Такой формат удобен, когда информацию нужно сравнивать: кто из какого отдела и какой у него email.',
      'После теории добавь строки с сотрудниками внутрь таблицы. Важно не потерять структуру table, tr, th и td.',
    ],
    brief: 'Добавь в таблицу строки <code>&lt;tr&gt;</code> с данными сотрудников: имя, отдел и email в ячейках <code>&lt;td&gt;</code>.',
    theory: `
      <b>Таблица — это данные по строкам и столбцам</b><br><br>
      Таблицы нужны, когда информацию удобно сравнивать: расписание уроков, список сотрудников, турнирная
      таблица, оценки по предметам. Если данные можно представить как «строки и столбцы», значит таблица
      подходит.<br><br>

      <b>Основные теги таблицы:</b><br>
      <code>&lt;table&gt;</code> — контейнер всей таблицы;<br>
      <code>&lt;tr&gt;</code> — строка таблицы;<br>
      <code>&lt;th&gt;</code> — заголовочная ячейка, обычно название столбца;<br>
      <code>&lt;td&gt;</code> — обычная ячейка с данными.<br><br>

      <b>Главная схема вложенности:</b><br>
      <pre><code>table
  tr
    th или td</code></pre>
      Ячейки не кладут сразу в <code>&lt;table&gt;</code>. Сначала создаётся строка
      <code>&lt;tr&gt;</code>, и только внутри неё размещаются ячейки.<br><br>

      <b>Пример:</b><br>
      <pre><code>&lt;table&gt;
  &lt;tr&gt;
    &lt;th&gt;Имя&lt;/th&gt;
    &lt;th&gt;Отдел&lt;/th&gt;
    &lt;th&gt;Email&lt;/th&gt;
  &lt;/tr&gt;
  &lt;tr&gt;
    &lt;td&gt;Иван&lt;/td&gt;
    &lt;td&gt;Разработка&lt;/td&gt;
    &lt;td&gt;ivan@example.ru&lt;/td&gt;
  &lt;/tr&gt;
&lt;/table&gt;</code></pre>

      <b>Как проверить себя:</b> если в таблице 2 строки данных и 3 столбца, то обычных ячеек
      <code>&lt;td&gt;</code> должно быть 6. Заголовки <code>&lt;th&gt;</code> считаются отдельно.
      Не используй таблицы для расположения кнопок и блоков на странице — для раскладки есть CSS.
    `,
    description: `
      ✏️ <b>Задание:</b> Добавь в таблицу две строки с данными сотрудников.<br><br>
      Заголовки уже есть. Добавь две строки <code>&lt;tr&gt;</code> с тремя ячейками <code>&lt;td&gt;</code> в каждой:<br><br>
      Строка 1: «Иван Петров», «Разработка», «ivan@kodlix.ru»<br>
      Строка 2: «Анна Смирнова», «Дизайн», «anna@kodlix.ru»
    `,
    starterCode: `<table>
  <tr>
    <th>Имя</th>
    <th>Отдел</th>
    <th>Email</th>
  </tr>
  <!-- добавь строки с данными здесь -->

</table>`,
    check(code) {
      const d = document.createElement('div');
      d.innerHTML = code;
      const table = d.querySelector('table');
      if (!table) return { ok: false, hint: 'Тег &lt;table&gt; не найден.' };
      const rows = table.querySelectorAll('tr');
      if (rows.length < 3) return { ok: false, hint: `Нужно минимум 3 строки (1 заголовок + 2 данных). Сейчас: ${rows.length}.` };
      const dataCells = table.querySelectorAll('td');
      if (dataCells.length < 6) return { ok: false, hint: `Нужно 6 ячеек &lt;td&gt; (2 строки × 3 столбца). Сейчас: ${dataCells.length}.` };
      const hasEmpty = Array.from(dataCells).some(td => !td.textContent.trim());
      if (hasEmpty) return { ok: false, hint: 'Все ячейки &lt;td&gt; должны содержать текст.' };
      return { ok: true };
    },
    reward: '🏅 Таблицы освоены! Саша говорит: «Огонь, спасибо!»',
    colleagueTips: [
      { npc: 'colleague2', lines: [
        'Саша здесь! Это моя задача, объясню.',
        'Каждая строка — это &lt;tr&gt;. Внутри строки — ячейки &lt;td&gt;. Три столбца = три &lt;td&gt; в каждой строке.',
        'Структура: &lt;tr&gt;&lt;td&gt;Иван&lt;/td&gt;&lt;td&gt;Разработка&lt;/td&gt;&lt;td&gt;email&lt;/td&gt;&lt;/tr&gt;',
      ]},
      { npc: 'colleague1', lines: [
        'Маша! Таблицы — это tr для строк и td для ячеек.',
        'Не путай th и td: th — заголовок (жирный), td — обычные данные.',
        'Добавь две строки tr, в каждой по три td с нужными данными.',
      ]},
    ],
  },

  // ── HTML ЗАДАНИЕ 5: Форма ─────────────────────────────────
  {
    id: 'html-5-forms',
    module: 'html',
    moduleIndex: 5,
    title: 'HTML: Форма обратной связи',
    npc: 'boss',
    context: `
      📝 <b>Артём вызывает тебя на серьёзный разговор:</b><br><br>
      <i>«Дима, ты хорошо справляешься. Даю тебе первое самостоятельное задание.
      Нам нужна форма обратной связи на сайте — поле для имени, поле для email,
      текстовое поле для сообщения и кнопка отправки.
      Это базовая вещь, которую должен уметь каждый фронтендер. Справишься?»</i><br><br>
      Ты уверенно киваешь. Пора применить всё, что узнал об HTML.
    `,
    introDialog: [
      'Дима, пользователям нужен способ написать нам сообщение прямо с сайта. Для этого нужна форма обратной связи.',
      'Форма — это не один тег, а набор связанных элементов: подписи, поля ввода, поле для большого текста и кнопка отправки.',
      'Если всё собрать аккуратно, пользователю будет понятно, что куда вводить, а браузер сможет правильно обработать данные.',
      'Пройди теорию про form, label, input и textarea, затем собери форму с именем, email, сообщением и кнопкой.',
    ],
    brief: 'Собери форму: <code>&lt;form&gt;</code>, поля для имени и email, <code>&lt;textarea&gt;</code> для сообщения и кнопку отправки.',
    theory: `
      <b>Форма — способ получить данные от пользователя</b><br><br>
      Почти любой сайт что-то спрашивает у пользователя: имя при регистрации, email, сообщение в поддержку,
      поисковый запрос. Для этого в HTML есть формы. Всё, что относится к одной форме, помещают внутрь
      тега <code>&lt;form&gt;</code>.<br><br>

      <b>Основные элементы формы:</b><br>
      <code>&lt;input type="text"&gt;</code> — короткий текст, например имя;<br>
      <code>&lt;input type="email"&gt;</code> — email; браузер понимает, что здесь ожидается адрес почты;<br>
      <code>&lt;textarea&gt;</code> — большое многострочное поле для сообщения;<br>
      <code>&lt;button type="submit"&gt;</code> — кнопка отправки формы.<br><br>

      <b>Почему type важен?</b><br>
      Атрибут <code>type</code> меняет поведение поля. Например, на телефоне для
      <code>type="email"</code> клавиатура может показать символ <code>@</code>, а браузер сможет
      подсказать пользователю, что email введён неправильно.<br><br>

      <b>Placeholder и label</b><br>
      <code>placeholder</code> — короткая подсказка внутри пустого поля. Она помогает, но не заменяет
      настоящую подпись.<br>
      <code>&lt;label&gt;</code> — подпись к полю. С ней форма понятнее и доступнее.<br>
      <pre><code>&lt;label for="name"&gt;Имя&lt;/label&gt;
&lt;input id="name" type="text" placeholder="Введи имя"&gt;</code></pre>

      <b>Минимальная форма обратной связи:</b><br>
      <pre><code>&lt;form&gt;
  &lt;input type="text" placeholder="Имя"&gt;
  &lt;input type="email" placeholder="Email"&gt;
  &lt;textarea placeholder="Сообщение"&gt;&lt;/textarea&gt;
  &lt;button type="submit"&gt;Отправить&lt;/button&gt;
&lt;/form&gt;</code></pre>

      <b>Важно:</b> поля и кнопка должны быть внутри <code>&lt;form&gt;</code>. Тогда браузер понимает,
      что это единый набор данных, который пользователь собирается отправить.
    `,
    description: `
      ✏️ <b>Задание:</b> Создай форму обратной связи с нуля.<br><br>
      Форма должна содержать:<br>
      1. Тег <code>&lt;form&gt;</code> как контейнер<br>
      2. <code>&lt;input type="text"&gt;</code> — поле для имени<br>
      3. <code>&lt;input type="email"&gt;</code> — поле для email<br>
      4. <code>&lt;textarea&gt;</code> — поле для сообщения<br>
      5. <code>&lt;button type="submit"&gt;</code> — кнопку с текстом «Отправить»
    `,
    starterCode: `<!-- Создай форму обратной связи здесь -->
`,
    check(code) {
      const d = document.createElement('div');
      d.innerHTML = code;
      const form = d.querySelector('form');
      if (!form) return { ok: false, hint: 'Оберни всё в тег &lt;form&gt;...&lt;/form&gt;.' };
      const inputText = form.querySelector('input[type="text"]');
      if (!inputText) return { ok: false, hint: 'Добавь &lt;input type="text"&gt; для имени.' };
      const inputEmail = form.querySelector('input[type="email"]');
      if (!inputEmail) return { ok: false, hint: 'Добавь &lt;input type="email"&gt; для email.' };
      const textarea = form.querySelector('textarea');
      if (!textarea) return { ok: false, hint: 'Добавь &lt;textarea&gt;&lt;/textarea&gt; для сообщения.' };
      const btn = form.querySelector('button[type="submit"]');
      if (!btn) return { ok: false, hint: 'Добавь &lt;button type="submit"&gt;Отправить&lt;/button&gt;.' };
      if (!btn.textContent.trim()) return { ok: false, hint: 'Кнопка должна содержать текст, например «Отправить».' };
      return { ok: true };
    },
    reward: '🏅 Формы освоены! Артём жмёт руку: «Добро пожаловать в команду, джун!»',
    colleagueTips: [
      { npc: 'colleague1', lines: [
        'Маша! Формы — это несложно, главное не забыть обернуть всё в &lt;form&gt;.',
        'Для имени: &lt;input type="text"&gt;, для email: &lt;input type="email"&gt;.',
        'Textarea — это отдельный тег, не input! И не забудь кнопку &lt;button type="submit"&gt;.',
      ]},
      { npc: 'colleague2', lines: [
        'Саша. Пять элементов: form, input text, input email, textarea, button submit.',
        'Все поля должны быть ВНУТРИ тега form.',
        'Кнопка: &lt;button type="submit"&gt;Отправить&lt;/button&gt; — не забудь текст внутри.',
      ]},
    ],
  },

  // ── HTML ЗАДАНИЕ 6: div, span, class, id ──────────────────
  {
    id: 'html-6-divspan',
    module: 'html',
    moduleIndex: 6,
    title: 'HTML: Блоки и классы',
    npc: 'colleague1',
    context: `
      🧩 <b>Маша зовёт тебя к своему монитору:</b><br><br>
      <i>«Дима, смотри — я делаю карточки новостей для корпоративного портала.
      Мне нужно сгруппировать элементы в блоки, чтобы потом стилизовать их через CSS.
      Ты знаешь про div и span? Помоги собрать структуру карточки.»</i><br><br>
      Это важный момент — именно div и span являются основой любой современной вёрстки.
    `,
    introDialog: [
      'Дима, на портале нужна карточка новости. В ней есть заголовок, текст, дата и ссылка, но сейчас это разрозненные элементы.',
      'Чтобы потом красиво оформить карточку через CSS, элементы нужно объединить в контейнер и дать ему понятный класс.',
      'Для большого блока подойдёт div, а для маленького кусочка текста внутри абзаца — span.',
      'После теории собери div с классом card, внутри размести заголовок, абзац с датой в span.date и ссылку “Читать далее”.',
    ],
    brief: 'Создай <code>&lt;div class="card"&gt;</code> с <code>&lt;h3&gt;</code>, абзацем, датой в <code>&lt;span class="date"&gt;</code> и ссылкой.',
    theory: `
      <b>Контейнеры помогают собирать страницу из блоков</b><br><br>
      На реальном сайте элементы часто нужно объединять: карточка новости содержит заголовок, текст,
      дату и ссылку; профиль содержит аватар, имя и описание. Для таких группировок используют
      контейнеры.<br><br>

      <b>&lt;div&gt; — блочный контейнер</b><br>
      <code>&lt;div&gt;</code> группирует крупные части страницы. По умолчанию он занимает всю доступную
      ширину и начинается с новой строки.<br>
      <pre><code>&lt;div class="card"&gt;
  &lt;h3&gt;Заголовок новости&lt;/h3&gt;
  &lt;p&gt;Краткое описание новости&lt;/p&gt;
&lt;/div&gt;</code></pre>

      <b>&lt;span&gt; — строчный контейнер</b><br>
      <code>&lt;span&gt;</code> используют, когда нужно выделить небольшой кусок внутри строки, не разрывая
      абзац на отдельные блоки.<br>
      <pre><code>&lt;p&gt;Дата публикации: &lt;span class="date"&gt;12 мая&lt;/span&gt;&lt;/p&gt;</code></pre>

      <b>class — имя для группы похожих элементов</b><br>
      Класс нужен, чтобы потом обращаться к элементу из CSS или JavaScript. Один и тот же класс можно
      дать многим элементам.<br>
      <pre><code>&lt;div class="card"&gt;Первая карточка&lt;/div&gt;
&lt;div class="card"&gt;Вторая карточка&lt;/div&gt;</code></pre>

      <b>id — уникальное имя одного элемента</b><br>
      <code>id</code> используют, когда элемент на странице должен быть один: например,
      <code>id="main-menu"</code> или <code>id="login-form"</code>. Повторять одинаковый
      <code>id</code> на одной странице нельзя.<br><br>

      <b>Как выбрать?</b><br>
      Для повторяющихся карточек, кнопок и дат чаще используй <code>class</code>. Для одного особенного
      элемента — <code>id</code>. В этом задании карточке нужен класс <code>card</code>, а дате внутри
      текста — класс <code>date</code>.
    `,
    description: `
      ✏️ <b>Задание:</b> Собери карточку новости для корпоративного портала.<br><br>
      Создай <code>&lt;div&gt;</code> с классом <code>card</code>, внутри которого:<br>
      1. <code>&lt;h3&gt;</code> с любым заголовком новости<br>
      2. <code>&lt;p&gt;</code> с текстом новости, где дата обёрнута в <code>&lt;span&gt;</code> с классом <code>date</code><br>
      3. <code>&lt;a&gt;</code> с атрибутом <code>href</code> и текстом «Читать далее»
    `,
    starterCode: `<!-- Собери карточку новости -->
`,
    check(code) {
      const d = document.createElement('div');
      d.innerHTML = code;
      const card = d.querySelector('div.card');
      if (!card) return { ok: false, hint: 'Создай &lt;div class="card"&gt;...&lt;/div&gt; как контейнер карточки.' };
      const h3 = card.querySelector('h3');
      if (!h3 || !h3.textContent.trim()) return { ok: false, hint: 'Внутри карточки добавь &lt;h3&gt; с заголовком новости.' };
      const p = card.querySelector('p');
      if (!p) return { ok: false, hint: 'Добавь &lt;p&gt; с текстом новости внутри карточки.' };
      const span = card.querySelector('span.date');
      if (!span) return { ok: false, hint: 'Оберни дату в &lt;span class="date"&gt;...&lt;/span&gt; внутри параграфа.' };
      const a = card.querySelector('a[href]');
      if (!a) return { ok: false, hint: 'Добавь &lt;a href="..."&gt;Читать далее&lt;/a&gt; внутри карточки.' };
      return { ok: true };
    },
    reward: '🏅 div и span освоены! Маша: «Теперь я смогу стилизовать это через CSS!»',
    colleagueTips: [
      { npc: 'colleague1', lines: [
        'Маша здесь! div — это коробка для блоков, span — для кусочка текста.',
        'class пишется так: &lt;div class="card"&gt;. Потом в CSS можно написать .card { ... }.',
        'Структура: div.card → h3, p (внутри p — span.date), a[href].',
      ]},
      { npc: 'colleague2', lines: [
        'Саша. div — блочный, span — строчный. Запомни это раз и навсегда.',
        'class="card" — это имя класса. Точка перед именем в CSS: .card { color: red; }.',
        'span.date — это span с классом date. Он должен быть ВНУТРИ тега p.',
      ]},
    ],
  },

  // ── HTML ЗАДАНИЕ 7: Семантика HTML5 ───────────────────────
  {
    id: 'html-7-semantic',
    module: 'html',
    moduleIndex: 7,
    title: 'HTML: Семантическая разметка',
    npc: 'boss',
    context: `
      🏗️ <b>Артём собирает команду на код-ревью:</b><br><br>
      <i>«Ребята, я смотрел вёрстку нашего нового лендинга — там везде одни div'ы.
      Это непрофессионально. Поисковики и скринридеры не понимают структуру страницы.
      Дима, переверстай шапку и подвал с использованием семантических тегов HTML5.
      Это важно для SEO и доступности.»</i><br><br>
      Ты открываешь файл. Пора познакомиться с семантикой.
    `,
    introDialog: [
      'Дима, на ревью всплыла типичная проблема новичков: страница собрана из одних div, и по коду трудно понять, где шапка, меню и подвал.',
      'Визуально такой сайт может работать, но для поисковиков, скринридеров и других разработчиков структура получается мутной.',
      'HTML5 даёт семантические теги: header, nav, main, footer. Они называют роль блока прямо в коде.',
      'Разберись с теорией и замени безымянные div на теги со смыслом. Содержимое блоков при этом менять не нужно.',
    ],
    brief: 'Замени нужные <code>&lt;div&gt;</code> на семантические теги: <code>&lt;header&gt;</code>, <code>&lt;nav&gt;</code>, <code>&lt;main&gt;</code>, <code>&lt;footer&gt;</code>.',
    theory: `
      <b>Семантика — это HTML со смыслом</b><br><br>
      Страницу можно собрать почти целиком из <code>&lt;div&gt;</code>, и визуально она может выглядеть
      нормально. Но для браузера, поисковой системы, программы экранного доступа и другого разработчика
      такие блоки будут безымянными коробками. Семантические теги сразу объясняют роль части страницы.<br><br>

      <b>Основные семантические теги:</b><br>
      <code>&lt;header&gt;</code> — шапка страницы или крупного раздела: логотип, название, верхняя часть;<br>
      <code>&lt;nav&gt;</code> — навигация, то есть меню со ссылками;<br>
      <code>&lt;main&gt;</code> — главное содержимое страницы; обычно на странице один
      <code>&lt;main&gt;</code>;<br>
      <code>&lt;section&gt;</code> — отдельный смысловой раздел;<br>
      <code>&lt;article&gt;</code> — самостоятельный материал: статья, пост, новость;<br>
      <code>&lt;aside&gt;</code> — боковая или дополнительная информация;<br>
      <code>&lt;footer&gt;</code> — нижняя часть: копирайт, контакты, дополнительные ссылки.<br><br>

      <b>Сравни два варианта:</b><br>
      <pre><code>&lt;div&gt;
  &lt;div&gt;
    &lt;a href="#"&gt;Главная&lt;/a&gt;
  &lt;/div&gt;
&lt;/div&gt;</code></pre>
      Такой код работает, но смысл блоков приходится угадывать.<br>
      <pre><code>&lt;header&gt;
  &lt;nav&gt;
    &lt;a href="#"&gt;Главная&lt;/a&gt;
  &lt;/nav&gt;
&lt;/header&gt;</code></pre>
      Здесь сразу ясно: это шапка, а внутри неё меню.<br><br>

      <b>Почему это важно?</b><br>
      Семантика помогает сайту быть доступнее, лучше индексироваться поисковиками и легче поддерживаться.
      Разработчик, который откроет такой код через месяц, быстрее поймёт структуру страницы.<br><br>

      <b>Правило выбора:</b> если у блока есть понятная роль, используй тег с этой ролью. Если подходящего
      смыслового тега нет, тогда можно использовать <code>&lt;div&gt;</code>.
    `,
    description: `
      ✏️ <b>Задание:</b> Переверстай страницу лендинга с семантическими тегами.<br><br>
      Замени все <code>&lt;div&gt;</code> на подходящие семантические теги:<br>
      1. Внешний блок с логотипом → <code>&lt;header&gt;</code><br>
      2. Блок с меню-ссылками → <code>&lt;nav&gt;</code><br>
      3. Блок с основным контентом → <code>&lt;main&gt;</code><br>
      4. Нижний блок с копирайтом → <code>&lt;footer&gt;</code>
    `,
    starterCode: `<div>
  <h1>Кодликс</h1>
  <div>
    <a href="#">Главная</a>
    <a href="#">О нас</a>
    <a href="#">Контакты</a>
  </div>
</div>

<div>
  <h2>Лучший поиск в рунете</h2>
  <p>Мы индексируем миллиарды страниц каждый день.</p>
</div>

<div>
  <p>© 2024 Кодликс. Все права защищены.</p>
</div>`,
    check(code) {
      const d = document.createElement('div');
      d.innerHTML = code;
      const header = d.querySelector('header');
      if (!header) return { ok: false, hint: 'Замени внешний &lt;div&gt; с логотипом на &lt;header&gt;.' };
      const nav = d.querySelector('nav');
      if (!nav) return { ok: false, hint: 'Замени блок с ссылками-меню на &lt;nav&gt;.' };
      const navLinks = nav.querySelectorAll('a');
      if (navLinks.length < 2) return { ok: false, hint: 'Внутри &lt;nav&gt; должны быть ссылки &lt;a&gt;.' };
      const main = d.querySelector('main');
      if (!main) return { ok: false, hint: 'Замени блок с основным контентом на &lt;main&gt;.' };
      const footer = d.querySelector('footer');
      if (!footer) return { ok: false, hint: 'Замени нижний блок с копирайтом на &lt;footer&gt;.' };
      return { ok: true };
    },
    reward: '🏅 Семантика освоена! Артём: «Вот теперь это профессиональная вёрстка.»',
    colleagueTips: [
      { npc: 'colleague2', lines: [
        'Саша. Семантика — это просто замена div на более говорящие теги.',
        'header — шапка, nav — навигация, main — главный контент, footer — подвал.',
        'Просто замени каждый &lt;div&gt; на нужный тег. Атрибуты и содержимое не трогай.',
      ]},
      { npc: 'colleague1', lines: [
        'Маша! Семантические теги — это как подписи на коробках. Сразу понятно что внутри.',
        'Блок с логотипом и меню → header. Меню со ссылками → nav. Основной текст → main. Копирайт → footer.',
        'Просто замени открывающий и закрывающий &lt;div&gt; на нужный тег.',
      ]},
    ],
  },

  // ── HTML ЗАДАНИЕ 8: Структура HTML-документа ──────────────
  {
    id: 'html-8-document',
    module: 'html',
    moduleIndex: 8,
    title: 'HTML: Скелет страницы',
    npc: 'boss',
    context: `
      📄 <b>Пятница. Артём ставит финальное задание недели:</b><br><br>
      <i>«Дима, ты уже умеешь писать отдельные HTML-элементы. Но настоящая страница —
      это не просто набор тегов. У неё есть чёткая структура: DOCTYPE, head, body.
      Создай с нуля полноценный HTML-документ для новой страницы нашего сайта.
      Без этого ни один браузер не поймёт твой код правильно.»</i><br><br>
      Это финальное задание HTML-модуля. Собери всё воедино.
    `,
    introDialog: [
      'Дима, финальное HTML-задание: теперь нужно собрать не фрагмент, а полноценный документ страницы.',
      'У настоящей страницы есть служебная часть и видимая часть. Браузеру нужно явно сказать стандарт HTML, язык, кодировку и название вкладки.',
      'Если пропустить каркас документа, страница может работать непредсказуемо, особенно в больших проектах.',
      'После теории собери страницу с DOCTYPE, html lang, head, meta charset, title и body с заголовком и абзацем.',
    ],
    brief: 'Создай полный HTML-документ: <code>DOCTYPE</code>, <code>html lang</code>, <code>head</code> с <code>charset</code> и <code>title</code>, затем <code>body</code> с <code>h1</code> и <code>p</code>.',
    theory: `
      <b>Полный HTML-документ — каркас веб-страницы</b><br><br>
      До этого ты писал отдельные фрагменты: заголовки, списки, таблицы, формы. Но настоящая страница
      обычно начинается с полного документа. Он сообщает браузеру, где служебная информация, где видимое
      содержимое и какой стандарт HTML используется.<br><br>

      <b>Минимальный современный шаблон:</b><br>
      <pre><code>&lt;!DOCTYPE html&gt;
&lt;html lang="ru"&gt;
  &lt;head&gt;
    &lt;meta charset="UTF-8"&gt;
    &lt;title&gt;Название страницы&lt;/title&gt;
  &lt;/head&gt;
  &lt;body&gt;
    &lt;h1&gt;Главный заголовок&lt;/h1&gt;
    &lt;p&gt;Первый абзац страницы.&lt;/p&gt;
  &lt;/body&gt;
&lt;/html&gt;</code></pre>

      <b>Что делает каждая часть:</b><br>
      <code>&lt;!DOCTYPE html&gt;</code> — первая строка документа. Она говорит браузеру: используй
      современный HTML5, а не старые режимы совместимости.<br><br>

      <code>&lt;html lang="ru"&gt;</code> — корневой элемент всей страницы. Атрибут
      <code>lang="ru"</code> сообщает, что основной язык страницы русский. Это важно для поиска,
      перевода страницы и программ чтения с экрана.<br><br>

      <code>&lt;head&gt;</code> — служебная часть. Её содержимое обычно не видно на странице, но оно
      влияет на работу документа: кодировка, название вкладки, подключение стилей и другие настройки.<br><br>

      <code>&lt;meta charset="UTF-8"&gt;</code> — кодировка. UTF-8 позволяет нормально отображать русский
      текст, английский текст, цифры и множество символов.<br><br>

      <code>&lt;title&gt;</code> — название вкладки браузера. Его также могут показывать поисковые системы
      и закладки.<br><br>

      <code>&lt;body&gt;</code> — видимая часть страницы. Именно сюда помещают заголовки, абзацы, картинки,
      таблицы, формы и кнопки.<br><br>

      <b>Порядок важен:</b> сначала <code>DOCTYPE</code>, затем <code>html</code>, внутри него
      <code>head</code> и <code>body</code>. Такой каркас делает страницу предсказуемой для браузера.
    `,
    description: `
      ✏️ <b>Задание:</b> Создай полноценный HTML-документ для страницы «Вакансии Кодликса».<br><br>
      Документ должен содержать:<br>
      1. <code>&lt;!DOCTYPE html&gt;</code><br>
      2. <code>&lt;html&gt;</code> с атрибутом <code>lang</code><br>
      3. <code>&lt;head&gt;</code> с <code>&lt;meta charset="UTF-8"&gt;</code> и <code>&lt;title&gt;</code><br>
      4. <code>&lt;body&gt;</code> с заголовком <code>&lt;h1&gt;</code> и хотя бы одним <code>&lt;p&gt;</code>
    `,
    starterCode: `<!-- Создай полноценный HTML-документ здесь -->
`,
    check(code) {
      const hasDoctype = /<!DOCTYPE\s+html>/i.test(code);
      if (!hasDoctype) return { ok: false, hint: 'Добавь &lt;!DOCTYPE html&gt; самой первой строкой.' };
      const hasHtmlLang = /<html[^>]+lang\s*=/i.test(code);
      if (!hasHtmlLang) return { ok: false, hint: 'Тег &lt;html&gt; должен иметь атрибут lang, например: &lt;html lang="ru"&gt;.' };
      const hasCharset = /charset\s*=\s*["']?UTF-8["']?/i.test(code);
      if (!hasCharset) return { ok: false, hint: 'Добавь в &lt;head&gt; тег &lt;meta charset="UTF-8"&gt;.' };
      const hasTitle = /<title>[^<]+<\/title>/i.test(code);
      if (!hasTitle) return { ok: false, hint: 'Добавь &lt;title&gt;Название страницы&lt;/title&gt; внутри &lt;head&gt;.' };
      const hasBody = /<body[\s>]/i.test(code);
      if (!hasBody) return { ok: false, hint: 'Добавь тег &lt;body&gt;...&lt;/body&gt; с содержимым страницы.' };
      const hasH1 = /<h1>[^<]+<\/h1>/i.test(code);
      if (!hasH1) return { ok: false, hint: 'Внутри &lt;body&gt; добавь &lt;h1&gt; с заголовком страницы.' };
      const hasP = /<p>[^<]+<\/p>/i.test(code);
      if (!hasP) return { ok: false, hint: 'Добавь хотя бы один &lt;p&gt; с текстом внутри &lt;body&gt;.' };
      return { ok: true };
    },
    reward: '🏅 HTML-модуль полностью пройден! Артём: «Теперь ты знаешь HTML как настоящий фронтендер. Следующий шаг — CSS!»',
    colleagueTips: [
      { npc: 'colleague1', lines: [
        'Маша! Структура документа — это как скелет. Без него всё рассыпается.',
        'Порядок: DOCTYPE → html[lang] → head[charset + title] → body[h1 + p].',
        'Не забудь lang="ru" у тега html и charset="UTF-8" у meta!',
      ]},
      { npc: 'colleague2', lines: [
        'Саша. Запомни шаблон раз и навсегда: DOCTYPE, html[lang], head[charset+title], body[контент].',
        'Без DOCTYPE браузер включает "режим совместимости" — страница может сломаться.',
        'Скопируй структуру из теории и заполни title, h1 и p своим текстом.',
      ]},
    ],
  },
];
